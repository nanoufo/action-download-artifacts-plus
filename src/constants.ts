export enum Inputs {
  Path = "path",
  Names = "names",
  IfNoArtifactsMatch = "if-no-artifacts-match",
}

export enum FailureHandlingStrategy {
  warn = "warn",
  error = "error",
  ignore = "ignore",
}
